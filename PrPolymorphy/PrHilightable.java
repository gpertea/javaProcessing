
public interface PrHilightable {
   boolean contains(float ax, float ay);
   boolean hilight(); //prepare the fill and stroke settings 
                 //for "highlighting" the object when the mouse pointer hovers above it
}
